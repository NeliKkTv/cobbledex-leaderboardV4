# CONFIG FILE (version corrigée pour exécution automatique GitHub + login via .txt)

[INPUT]

# Méthode d'entrée : manual, local, ftp, sftp
Mode = sftp

## LOCAL (inutile ici)
LocalPath = null

## FTP et SFTP
# Adresse du serveur (IP ou domaine)
Host = 109.239.157.170
# Port de connexion SFTP (souvent 22)
Port = 22

# ⚠️ Les identifiants doivent être placés dans deux fichiers à la racine du projet :
# - username.txt  → contient UNIQUEMENT ton nom d'utilisateur SFTP (ex: 71749-File)
# - password.txt  → contient UNIQUEMENT ton mot de passe SFTP
#
# Ne mets pas ces fichiers dans le repo public : ajoute-les dans le dossier d'exécution GitHub Actions ou sur ton PC.
#
# Chemin vers le dossier racine du serveur (celui contenant world/, stats/, cobblemonplayerdata/, etc.)
FTPPath = /Minecraft/world


[GITHUB]
# Upload automatique vers ton repo GitHub public
Enable = true

# Le token personnel n'est plus nécessaire : GitHub Actions injecte automatiquement son token d'exécution.
# Laisse "auto" — le script utilisera le token GitHub intégré pour le commit/push.
Token = auto

# Informations du dépôt
RepoOwner = NeliKkTv
RepoName = cobbledex-leaderboardV4
Branch = main


[VANILLALEADERBOARD]
Enable = false
Category = minecraft:custom
Subcategory = minecraft:pokemon_registered
CreateCSV = true
CSVPath = total.csv
CreateCSVMoney = false
CSVPathMoney = money.csv


[TOPIMAGE]
Enable = true
NbPlayers = 20
Width = 2
Height = 1
ImagePath = images/top3.png
Leaderboards = vanilla/minecraft:custom/minecraft:pokemon_registered
Titles = Leaderboard
